package guiTest;

public class Random {
	/* List of things needed to randomize (brainstorm)
	 * Advance time only by user
	 * Adding Members (which means, RANDOMIZING NAMES AHHHH)
	 * Removing Members (more rare-ish)
	 **/
}
